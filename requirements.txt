# Core frameworks - без torchvision (устанавливается отдельно)
streamlit>=1.28.0
folium>=0.15.0
streamlit-folium>=0.15.0
onnxruntime>=1.15.0

# Image processing
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Data visualization
matplotlib>=3.7.0

# Utilities
requests>=2.31.0
gdown>=5.0.0

# Data processing
pandas>=2.0.0

# Remove torchvision - оно вызывает конфликты
# Вместо этого используем ultralytics, который сам установит нужные версии
ultralytics>=8.0.200

# Для работы с геоданными
rio-tiler>=6.0.0